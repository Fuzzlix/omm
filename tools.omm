--
-----------------------------------------------------------------------------------------------
--
for _, m in ipairs(make.utils.get_directories("tools")) do
  if make.utils.isFile(m..".omm") then
    make(m);
  end;
end;
